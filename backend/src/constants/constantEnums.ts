// have an enum with all the folder names
export enum ConstantEnums {
  AVATAR = "avatars",
  COVER_IMAGE = "cover_images",
  VIDEO = "videos",
  TUBE_STREAM = "tube-stream",
  USERS = "users",
  TUBESTREAM = "tubestream",
}
